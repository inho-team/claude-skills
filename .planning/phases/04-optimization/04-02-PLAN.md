---
phase: 04-optimization
plan: 04-02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - skills/Qgenerate-spec/SKILL.md
  - scripts/benchmark-qgen.sh
  - scripts/verify-memo.mjs
autonomous: true
requirements: [OPT-01]
must_haves:
  truths:
    - "Qgenerate-spec uses Haiku for drafting (Step 2) and structural checks (S1-S5 in Step 2.5)."
    - "Benchmarking script successfully measures Qgenerate-spec execution time."
    - "Memo consistency check confirms cached content matches disk content."
  artifacts:
    - path: "skills/Qgenerate-spec/SKILL.md"
      provides: "Updated instructions for Haiku offloading"
    - path: "scripts/benchmark-qgen.sh"
      provides: "Measurement tool for Qgenerate-spec performance"
    - path: "scripts/verify-memo.mjs"
      provides: "Validation for ContextMemo consistency"
---

<objective>
Optimize the `Qgenerate-spec` skill by offloading drafting and structural verification tasks to the Haiku model. Establish benchmarking and verification scripts to measure performance gains and ensure cache integrity.

Purpose: Achieve 40%+ faster spec generation and verify the reliability of the ContextMemo implementation.
Output: Optimized skill definition and performance/consistency verification suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-optimization/04-RESEARCH.md
@.planning/phases/04-optimization/04-01-SUMMARY.md
@skills/Qgenerate-spec/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize Qgenerate-spec with Haiku Offloading</name>
  <files>skills/Qgenerate-spec/SKILL.md</files>
  <action>
    Refactor the `Workflow` section in `skills/Qgenerate-spec/SKILL.md` to leverage Haiku for less complex reasoning tasks:
    - **Step 2 (Drafting)**: Explicitly instruct the agent to use the Haiku model (`model: haiku` in Agent tool) for drafting standard documents (TASK_REQUEST, VERIFY_CHECKLIST). Reserve Sonnet only for complex architectural sections (e.g., ADR or niche tech stack decisions).
    - **Step 2.5 (Verification)**: Update the "Spec Verification" logic to delegate Structural criteria checks (S1-S5) to a Haiku sub-agent. Maintain Sonnet for Executability criteria (E1-E4) which require higher reasoning.
    - Specify that these checks should happen in parallel if possible.
  </action>
  <verify>
    <automated>grep -E "haiku|Haiku" skills/Qgenerate-spec/SKILL.md</automated>
  </verify>
  <done>Skill definition updated to use model-tiering for optimization.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Performance Benchmarking Script</name>
  <files>scripts/benchmark-qgen.sh</files>
  <action>
    Create a bash script `scripts/benchmark-qgen.sh` that:
    1.  Records start time.
    2.  Invokes a mock `/Qgenerate-spec` flow (using a standardized project description).
    3.  Records end time and calculates duration.
    4.  Logs model usage (by checking `.qe/state/unified-state.json` or logs).
    5.  Outputs a comparison against a baseline (approx. 60-90s for Sonnet-only).
  </action>
  <verify>
    <automated>bash scripts/benchmark-qgen.sh --dry-run</automated>
  </verify>
  <done>Benchmarking script created and able to measure execution time.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Memo Consistency Verification</name>
  <files>scripts/verify-memo.mjs</files>
  <action>
    Create a Node.js script `scripts/verify-memo.mjs` that:
    1.  Reads `unified-state.json`.
    2.  Iterates through all entries in the `memo` object.
    3.  For each entry, compares the cached content with the actual file content on disk.
    4.  Reports any discrepancies (stale cache) or size limit violations.
    5.  Exits with code 1 if any inconsistency is found.
  </action>
  <verify>
    <automated>node scripts/verify-memo.mjs</automated>
  </verify>
  <done>Verification script created and confirms ContextMemo reliability.</done>
</task>

</tasks>

<verification>
Run `scripts/benchmark-qgen.sh` and confirm that execution time has decreased compared to historical baseline.
Run `scripts/verify-memo.mjs` after a session to confirm all cached files are consistent with disk.
</verification>

<success_criteria>
- [ ] `Qgenerate-spec` instructions updated with model-tiering (Haiku for Steps 2 & 2.5).
- [ ] `benchmark-qgen.sh` provides accurate timing data.
- [ ] `verify-memo.mjs` passes consistently across multiple tool calls.
- [ ] 40%+ reduction in `Qgenerate-spec` execution time observed in benchmarks.
</success_criteria>

<output>
After completion, create `.planning/phases/04-optimization/04-02-SUMMARY.md`
</output>
