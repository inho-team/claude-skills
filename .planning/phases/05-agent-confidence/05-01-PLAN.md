---
phase: 05-agent-confidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/Qrun-task/SKILL.md
  - skills/Qgenerate-spec/SKILL.md
  - skills/Qcode-run-task/SKILL.md
  - skills/Qcommit/SKILL.md
  - skills/Qcompact/SKILL.md
  - core/AGENT_BASE.md
  - core/AGENT_TEAMS.md
files_created:
  - skills/CATALOG.md
autonomous: true
requirements:
  - SKILL-TRIGGERS
  - SKILL-FIRST-POLICY
  - INTELLIGENT-HANDOFF
---

<objective>
Improve agent confidence and skill utilization by standardizing invocation triggers, mandating a "Skill-First" approach, and refining inter-agent handoff communication.

Purpose: To ensure agents proactively use specialized workflows (skills) instead of inefficient manual labor.
Output: Standardized skill metadata, a centralized skill catalog, and updated framework policies.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Standardize Skill Invocation Triggers</name>
  <files>
    - skills/Qrun-task/SKILL.md
    - skills/Qgenerate-spec/SKILL.md
    - skills/Qcode-run-task/SKILL.md
    - skills/Qcommit/SKILL.md
    - skills/Qcompact/SKILL.md
  </files>
  <action>
    Add `invocation_trigger: "{description}"` to the YAML frontmatter of the target skills.
    - Qrun-task: "When a TASK_REQUEST or checklist needs implementation or verification."
    - Qgenerate-spec: "When a new project, task, or bug fix spec needs to be defined."
    - Qcode-run-task: "When code has been modified and needs a quality loop (test-review-fix)."
    - Qcommit: "When changes are ready to be staged and committed to git."
    - Qcompact: "When the context window is full or under pressure (Orange/Red zone)."
  </action>
  <verify>grep "invocation_trigger" skills/Q*/SKILL.md</verify>
  <done>Core skills have standardized triggers defined.</done>
</task>

<task type="auto">
  <name>Task 2: Create Centralized Skill Catalog</name>
  <files>skills/CATALOG.md</files>
  <action>
    Create a high-signal `skills/CATALOG.md` file that lists all available skills, their `invocation_trigger`, and core benefits.
    Include a "Mandatory Read" directive at the top for agents.
  </action>
  <verify>ls skills/CATALOG.md</verify>
  <done>Skills are discoverable via a centralized high-signal catalog.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Skill-First Mandate</name>
  <files>core/AGENT_BASE.md</files>
  <action>
    Add a new rule to the "Will" section: "Prioritize skills over manual labor. Before implementing, refactoring, or documenting manually, search `skills/CATALOG.md` for a matching skill."
  </action>
  <verify>grep "Skill-First" core/AGENT_BASE.md</verify>
  <done>The framework policy now explicitly favors skill utilization.</done>
</task>

<task type="auto">
  <name>Task 4: Refine Handoff Packet Standard</name>
  <files>core/AGENT_TEAMS.md</files>
  <action>
    Update the Handoff Packet schema to include `expected_outcome` and `known_constraints` fields to improve execution intent and reduce agent drift.
  </action>
  <verify>grep "expected_outcome" core/AGENT_TEAMS.md</verify>
  <done>Agent handoff communication is more intelligent and outcome-oriented.</done>
</task>

</tasks>

<success_criteria>
- All 5 core skills have clear invocation triggers in their frontmatter.
- `skills/CATALOG.md` exists and contains a searchable list of skills.
- `core/AGENT_BASE.md` contains the "Skill-First" mandate.
- `core/AGENT_TEAMS.md` defines a more detailed handoff packet structure.
</success_criteria>
